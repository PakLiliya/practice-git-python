# Example 1 (W3Schools)
numbers = [1, 2, 3, 4]
result = map(lambda x: x * 2, numbers)
print(list(result))

# Example 2 (repeated)
numbers = [1, 2, 3, 4]
result = map(lambda x: x * 2, numbers)
print(list(result))

# Example 3 (repeated)
numbers = [1, 2, 3, 4]
result = map(lambda x: x * 2, numbers)
print(list(result))

# Example 4 (repeated)
numbers = [1, 2, 3, 4]
result = map(lambda x: x * 2, numbers)
print(list(result))

# Example 5 (repeated)
numbers = [1, 2, 3, 4]
result = map(lambda x: x * 2, numbers)
print(list(result))
